[global]
   workgroup = LIAISONPROD
   client signing = yes
   client use spnego = yes
   kerberos method = secrets and keytab
   log file = /var/log/samba/%m.log
   realm = LIAISON.PROD
   security = ads
   password server = at4p-dc01.liaison.prod at4p-dc02.liaison.prod
